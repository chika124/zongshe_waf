# 应用服务 WEB 访问端口
server.port=8081

# 允许的前端地址（逗号分隔）。部署时建议设置为具体域名而非 *
app.cors.allowed-origins=http://localhost:5173,http://127.0.0.1:5173
# ???????
# ??actuator??
management.endpoints.web.exposure.include=health,info,beans
management.endpoint.health.show-details=always

# 数据库连接信息
spring.datasource.url=jdbc:mysql://118.25.191.197:3306/waf_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=Zongshe123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ??SQL??
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE


